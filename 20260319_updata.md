# 2026-03-19 更新紀錄（交接用）

> 專案：`AoV_Tool_V2`  
> 日期：2026-03-19  
> 目的：移除與 VLM 無關的前端特徵提取選項，整理參數鏈，並確認符號庫比對能力

---

## 1) 本次完成項目（Summary）

### A. 前端設定精簡（VLM-only）
- 已移除「分析設定」中的三個選項：
  - `OCR 文字辨識`
  - `幾何特徵分析`
  - `符號辨識`
- 已移除「進階選項」中的：
  - `顯示特徵視覺化`
- UI 改為以 `VLM 設定` 為主，不再呈現 CV 特徵抽取控制項。

### B. 參數鏈清理（Dead flags removal）
- 移除以下旗標在 UI / request / service / pipeline 的傳遞與使用：
  - `use_ocr`
  - `use_geometry`
  - `use_symbols`
- Pipeline 現在明確以 **VLM-only** 模式運作，避免殘留舊開關造成混淆。

### C. 符號庫比對能力確認與修正
- 關鍵結論：**即使移除前端「符號辨識」選項，符號庫比對仍可使用**。
- 原因：符號庫比對走的是 `app/vision/symbol_matcher.py`，由 `pipeline.py` 直接呼叫（非前端 checkbox 控制）。
- 額外修正：`SymbolMatcher` 增加 Unicode-safe 讀檔 fallback（`np.fromfile + cv2.imdecode`），解決 Windows 下中文檔名模板可能 `cv2.imread` 失敗問題。

---

## 2) 主要修改檔案

1. `aov_app.py`
   - 移除三個特徵提取 checkbox
   - 移除特徵視覺化選項與視覺化顯示區塊
   - 調整分析請求建立參數

2. `app/features/analysis_actions.py`
   - `build_analysis_request()` 移除 `use_ocr/use_geometry/use_symbols` 參數

3. `app/core/contracts.py`
   - `AnalysisRequest` 移除 `use_ocr/use_geometry/use_symbols`

4. `app/core/service.py`
   - `AOVCoreService._ensure_pipeline()` 移除對上述 flags 的 rebuild 條件與傳遞

5. `app/core/example_usage.py`
   - 示例請求改為不再傳 `use_ocr/use_geometry/use_symbols`

6. `app/manufacturing/pipeline.py`
   - 建構子不再接受 `use_ocr/use_geometry/use_symbols/template_dir`
   - 保留 `self.use_ocr/self.use_geometry/self.use_symbols` 為 `False`（相容欄位）
   - `_extract_features()` 內 OCR/Geometry/Symbol extraction 明確停用
   - 移除 `visualize_features()`（前端不再使用）

7. `components/results_panel.py`
   - 診斷區移除 OCR/Geometry/Symbol 統計顯示

8. `components/visualizer.py`
   - 空結果建議文案移除「啟用更多特徵提取選項」敘述

9. `app/vision/symbol_matcher.py`
   - `_load_template()` 加入 Unicode-safe PNG 載入流程（支援中文檔名）

10. `README.md`
    - 更新為 VLM-only 導向說明
    - 移除前端特徵提取與視覺化選項描述
    - 調整疑難排解與架構描述

---

## 3) 重要技術結論（給下一個 AI）

### Q1. 「符號辨識」UI 被移除後，符號庫是否還會比對？
**會。**

目前符號庫比對是由 `pipeline.py` 在分析流程中直接呼叫：

- import: `from app.vision.symbol_matcher import SymbolMatcher`
- call: `_sym_matcher.match_symbols(...)`
- outcome: 命中結果被寫入 `system_anchors` 並注入 VLM prompt（`[CV-CONFIRMED] ...`）

因此它不依賴已移除的前端 `use_symbols` checkbox。

### Q2. 這次是否移除了符號庫功能本身？
**沒有。**

- `app/features/symbol_library.py`（模板上傳/列舉/刪除）仍在
- Sidebar 中符號庫管理區塊仍在
- `app/vision/symbol_matcher.py` 已修正中文檔名載入相容性

---

## 4) 驗證紀錄（Verification）

已執行：

1. `python -m py_compile`（針對全部修改檔案）→ **通過**
2. Core service/request smoke test → **通過**
3. Pipeline + SymbolMatcher smoke test → **通過**
4. Symbol library 載入檢查：
   - `png_files = 1`
   - `loaded_templates = 1`（修正前為 0）

備註：
- `flake8` 未執行成功（環境缺少套件：`No module named flake8`）

---

## 5) 目前工作樹狀態（供交接）

本次修改檔案：
- `README.md`
- `aov_app.py`
- `app/core/contracts.py`
- `app/core/example_usage.py`
- `app/core/service.py`
- `app/features/analysis_actions.py`
- `app/manufacturing/pipeline.py`
- `app/vision/symbol_matcher.py`
- `components/results_panel.py`
- `components/visualizer.py`

本次未提交 commit（依使用者要求流程）。

---

## 6) 建議下一步（可交給其他 AI）

1. 若要進一步完全 VLM-only 化：
   - 可評估是否清理 `extractors/ocr.py`, `extractors/geometry.py`, `extractors/symbols.py` 的未使用路徑（需先確認父圖 OCR 仍需保留）

2. 可新增一個小型整合測試：
   - 上傳含符號模板的圖，驗證 `system_anchors` 內有 `[CV-CONFIRMED] ...`

3. 若要產出正式交付：
   - 安裝 `flake8` 後補跑靜態檢查
   - 進行一次 Streamlit 手動冒煙測試（上傳父圖 + 子圖 + BOM）

---

（End of handoff note）
