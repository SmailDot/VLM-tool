{
  "task_title": "修復 HITL Session State 衝突與 VLM 輕量化精準 Prompt (Few-Shot & 尺寸防護)",
  "objective": "1. 修復 Streamlit 文字框雙重賦值的警告。 2. 全面改寫 VLM Prompt，採用『Few-Shot 範例引導』取代複雜規則。 3. 限制 20 個核心工程詞彙，並加入『嚴格禁止測量尺寸』的防護機制，確保小模型輸出正確的顏色標籤且不產生幻覺。",
  "target_files": [
    "aov_app.py",
    "app/manufacturing/prompts.py"
  ],
  "implementation_steps": [
    {
      "step": 1,
      "module": "aov_app.py",
      "description": "修復 Session State 雙重賦值警告 (Warning)。找到渲染 HITL 修正區的 `st.text_area`。移除其 `value=` 參數。正確的做法是：在顯示 text_area 之前，檢查 `if 'hitl_text' not in st.session_state: st.session_state.hitl_text = raw_vlm_description`。然後直接呼叫 `st.text_area('專家修正區', key='hitl_text')`，讓 Streamlit 完全透過 session_state 來管理該元件的值。"
    },
    {
      "step": 2,
      "module": "app/manufacturing/prompts.py",
      "description": "精簡 `ALLOWED VOCABULARY`，只保留視覺特徵，不要給加工指示。替換為：『Shapes: Rectangular Base, L-shaped Bracket, U-shaped Channel. Features: Flange (bent edge), Rib (support bump), Chamfer (cut corner), Fillet (rounded corner). Details: Thru-hole, Threaded hole, Extruded hole (Burring), Notch. Symbols: Weld symbol.』"
    },
    {
      "step": 3,
      "module": "app/manufacturing/prompts.py",
      "description": "新增強烈的尺寸防護指令 (Anti-Dimension Hallucination)。在 Prompt 中加入：『CRITICAL RULE: DO NOT attempt to read or guess specific dimensional values (like 110mm, 30mm, 55mm). You MUST ONLY describe the relative shapes. Writing specific millimeter measurements is a CRITICAL ERROR.』"
    },
    {
      "step": 4,
      "module": "app/manufacturing/prompts.py",
      "description": "移除原本冗長的『CONFIDENCE TAGGING RULES』，改為提供明確的 Few-Shot 輸出範本，讓模型直接模仿。加入以下內容：\n『OUTPUT TEMPLATE (You MUST strictly mimic this format and DO NOT output my rules):\n### 1. OVERALL 3D SHAPE\nThis is a <green>L-shaped Bracket</green>.\n### 2. COMPONENT STRUCTURE\nIt consists of a <green>Rectangular Base</green> and a <orange>Flange</orange>...』"
    }
  ],
  "acceptance_criteria": [
    "前端 Streamlit 介面不再出現 `created with a default value but also had its value set via the Session State API` 的警告。",
    "使用者在 text_area 中修改的內容能穩定保留，不會因為重整畫面而消失。",
    "VLM 的輸出文字中，成功包含 `<green>` 等 XML 標籤，讓前端成功渲染出紅綠橘等顏色。",
    "VLM 的報告中不再包含『110mm』、『50mm』等具體尺寸的幻覺測量數據。",
    "VLM 不會再把 Prompt 裡面的規則或大綱當作報告內容印出來。"
  ]
}